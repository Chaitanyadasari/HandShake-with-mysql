import React from 'react';
import SearchBar from './SearchBar';
import Menu from './Menu';

const Header = () => {
    return (
        <div>
            <SearchBar />
            <Menu />
        </div>
    );
}

export default Header;